{
  "name": "commercetools",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "lint": "eslint .",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "eslint-config-custom": "*",
    "nodemon-config": "*",
    "tsconfig": "*"
  }
}
